var filter-controller = require('../app/filter-controller.js');

describe("Servers", function() {
  describe("Inital render", function() {
    it("should render inital set of items", function() {

      //expect(testOperator).to.have.property('id');
      //expect(testOperator.id).to.equal('test');
    });
  });

});

